# 节能行为验证

## 1.4.1

验证日期：2026-09-05。30 项模型检查、27 项运行检查通过。模拟不响应 SIGTERM 的查询进程约 0.438 秒内清理完毕；普通阻塞读取取消约 0.121 秒；重复查询前后文件描述符为 4 → 4。供电切换不会覆盖短暂冷却中的刷新请求。正常退出应用异步等待查询清理。

## 1.4.0

验证日期：2026-09-05。30 项模型检查和 18 项运行检查通过。查询取消可立即唤醒阻塞等待；模拟重复查询前后管道句柄未累积；实际应用查询完成后无常驻查询子进程。发布改用 Release，主程序文件减少 35.8%。完整问题、证据和测量边界见 [REVIEW.md](REVIEW.md)。

## 1.3.1

验证日期：2026-09-05。

- 26 项模型和策略检查通过；正常查询固定为插电 60 秒、电池 300 秒，保留休眠、离线和失败退避检查。
- 本机安装版本为 1.3.1。原生界面已无“查看时”和频率选择器，刷新、更新时间和退出完整显示在同一行。
- 当前插电状态的原生提示为“每 1 分钟自动更新”。
- 查询完成后，即使菜单仍展开，采样确认只有一个应用主进程，查询子进程数量为 0。
- 电池、休眠和离线分支使用独立策略检查验证，未更改用户的系统设置。

## 1.3.0

版本：1.3.0。验证日期：2026-09-05。

- 26 项模型和策略检查通过，包含插电、电池、低电量、休眠、屏幕熄灭、非活跃会话、离线和失败退避。
- 使用本机系统电源通知确认当前状态为插电，界面正确显示菜单收起时约 2 分钟更新的策略。
- 原生界面确认每张重置卡均显示官方返回的到期日期，未知日期不会推断为永久有效。
- 实际收起菜单后，在相隔 45 秒的两次采样中，查询子进程数量均为 0。
- 这 45 秒内应用累计 CPU 时间两次均为 0.98 秒，采样时 CPU 使用率均为 0.0%。

这是一段后台空闲样本，用于确认没有持续计算或常驻查询子进程；没有据此估计整机续航增幅。未为测试而切换用户的电池设置、关闭网络或让电脑进入休眠；这些分支通过独立策略检查验证。
