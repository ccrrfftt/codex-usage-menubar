# 节能行为验证

版本：1.3.0。验证日期：2026-09-05。

- 26 项模型和策略检查通过，包含插电、电池、低电量、休眠、屏幕熄灭、非活跃会话、离线和失败退避。
- 使用本机系统电源通知确认当前状态为插电，界面正确显示菜单收起时约 2 分钟更新的策略。
- 原生界面确认每张重置卡均显示官方返回的到期日期，未知日期不会推断为永久有效。
- 实际收起菜单后，在相隔 45 秒的两次采样中，查询子进程数量均为 0。
- 这 45 秒内应用累计 CPU 时间两次均为 0.98 秒，采样时 CPU 使用率均为 0.0%。

这是一段后台空闲样本，用于确认没有持续计算或常驻查询子进程；没有据此估计整机续航增幅。未为测试而切换用户的电池设置、关闭网络或让电脑进入休眠；这些分支通过独立策略检查验证。
