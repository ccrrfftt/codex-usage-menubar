# 代码设计审查与优化记录

范围：1.3.1 → 1.4.0；日期：2026-09-05。审查覆盖查询进程、刷新调度、状态发布、原生界面、额度解析和发布构建。

已处理的主要问题：

| 优先级 | 原设计的问题 | 修改与证据 |
| --- | --- | --- |
| P2 | 停止操作和阻塞读取共用串行队列，只能排在读取完成或超时之后 | 查询改为独立会话，取消信号通过管道立即唤醒等待；模拟卡住的进程验证取消小于 1 秒，随后退出 |
| P2 | 一次成功查询分别修改多个发布属性，反复排队更新菜单栏 | 合并为开始、完成两次状态发布；菜单栏先去重再更新，回归检查确认新数据在完成时一次性提交 |
| P2 | 休眠、断网后旧请求可能迟到，恢复时存在新旧请求交错 | 只保留一个刷新任务，取消后的结果不能提交；覆盖离线、重连和迟到回复的检查 |
| P2 | 发布脚本使用 Debug 构建 | 打包固定使用 Release；日常调试仍保留 Debug；安装后的主程序体积缩小 35.8% |
| P2 | 其他额度缺少 limitId 或 limitId 冲突时可能被识别为主额度 | 以 rateLimitsByLimitId 的键作为权威标识；验证缺失、冲突和多额度优先级 |
| P2 | 极大但有限的周期或重置时间可能在转整数时崩溃 | 在转换前检查数值范围；增加异常时间值检查 |
| P3 | 卡片排序、额度分组、主额度筛选随视图取值重复执行 | 排序和分组移到后台解码阶段；主额度选择直接比较两个窗口，不再构造筛选数组 |
| P3 | 查询结果先转字典、再编码成 JSON、再解码 | 直接从响应解码成结果模型；握手和额度查询共用 25 秒总读取期限 |
| P3 | 菜单可见状态、重复停止逻辑、空通知别名和每次重建图标没有必要 | 删除冗余状态及重复逻辑；图标输入未变化时复用构建结果 |

界面拆成主布局、主额度、重置卡、其他额度四个视图；只有根视图观察状态，子视图接收明确的数据。没有新增依赖或后台常驻服务。配套个人插件移除了已退休的网页、Python 服务和旧测试；本地保留了删除前的备份。

验证结果：

| 项目 | 结果 | 解释与限制 |
| --- | --- | --- |
| 安装后的主程序文件 | 970,576 → 623,120 字节，减少 35.8% | 指可执行文件，不是整个应用包或运行内存 |
| 单次成功查询的状态发布 | 7 处属性写入 → 2 次完整状态发布 | 旧值按 1.3.1 源码静态计数；新值由订阅计数检查验证，不等同于画面实际重绘次数 |
| 模型和策略检查 | 30 项通过 | 额度语义、卡片信息、异常值、固定供电频率、暂停和退避 |
| 运行检查 | 18 项通过 | 并发去重、状态发布、取消、重连、迟到结果、超时和资源释放 |
| 重复模拟查询 | 8 次查询前后文件描述符数量为 4 → 4 | 独立测试进程中的短样本，未发现管道句柄累积 |
| 卡住的模拟查询 | 取消后小于 1 秒返回，进程退出，后续查询成功 | 使用本地模拟程序，不连接真实账户 |
| 本机原生应用 | 1.4.0 启动、真实查询、手动刷新、折叠、退出、重新打开通过 | 顶部位置和底部布局保持正常，查询完成后采样无查询子进程 |

正常更新频率继续保持插电 60 秒、使用电池 300 秒；系统或屏幕休眠、用户会话不活跃、离线时暂停；失败逐步退避。没有切换用户的供电、网络或休眠设置，这些分支由策略及运行检查验证。

本次没有进行受控的整机功耗、长时间内存或续航对照测试，因此不宣称具体的耗电或续航改善比例。已确认的是程序体积减少、状态发布次数减少，以及查询取消和资源释放行为得到验证。
