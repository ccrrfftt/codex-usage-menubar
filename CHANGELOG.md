# 更新记录

## 1.5.0

- 面板右上角新增「EN / 中文」一键切换，并保存本应用的语言选择。
- 标题、额度周期、重置倒计时、卡片到期日期、错误、菜单栏提示和辅助功能文本支持中英文。
- 切换语言立即生效，不重新查询额度，不改变更新计划；进行中的查询也保留最新语言选择。
- 增加英文项目首页，中文版与英文版可以互相切换。

## 1.4.1

- 修复查询进程不响应退出信号时可能残留的问题，增加有时限的退出清理。
- 正常退出应用时等待当前查询完成清理，避免主程序先结束。
- 修复供电状态变化可能推迟冷却中手动刷新的问题。
- 先核对响应标识，再解码对应结果，避免将无关响应误报为数据损坏。
- 定时器直接在主运行循环执行，减少一次不必要的异步排队。
- 检查扩充到 30 项模型检查和 27 项运行检查。

## 1.4.0

- 发布包改用 Release 优化构建，图标未变化时复用构建产物。
- 合并刷新状态通知，菜单栏订阅只接收有变化的显示数据。
- 查询使用可取消的独立会话；休眠、离线和退出不再排队等待读取超时。
- 握手和用量读取共用一个 25 秒总超时，直接解码结果，删除 JSON 字典中转。
- 新数据解码时一次完成额度分组与重置卡排序，拆分详情视图职责。
- 修复额度标识缺失或冲突时的分组，以及异常大时间值导致的整数转换问题。
- 增加并发刷新、取消、超时、重连、状态通知和资源释放的回归检查。

## 1.3.1

- 固定为插电每 1 分钟、使用电池每 5 分钟更新，菜单展开与收起采用相同频率。
- 移除“查看时”和频率下拉框，将刷新、更新时间和退出合并为一行。
- 每次查询结束后释放查询子进程，菜单展开时也不保持闲置连接。
- 保留休眠、熄屏、非活跃会话和离线暂停，以及失败后的逐步退避。

## 1.3.0

- 显示每张可用重置卡的到期时间，按最早到期排序；未知信息明确标注。
- 取消 5 秒界面时钟，使用单个允许系统合并唤醒的查询定时器。
- 菜单关闭时按插电、电池和低电量模式降低查询频率。
- 系统或屏幕休眠、会话不活跃、离线时停止自动查询，恢复后同步。
- 后台查询结束和菜单关闭时释放查询子进程；失败时增加退避时间。
- 避免重复改写未发生变化的菜单栏文字。

## 1.2.0

- 增加可用重置卡数量，直接采用官方可用次数。
- 菜单栏百分比字号比系统默认小 1 pt。
- “其他额度”整行可点击展开与折叠，取消弹窗缩放和折叠动画，顶部内容保持原位。
- 压缩纵向布局，合并刷新及更新频率控制。
- 移除“暂停”和“在访达中显示应用”。

## 1.1.0

- 菜单栏改为 Codex 图标 + 百分比，移除 Codex 文字前缀。
- 使用 macOS 原生状态栏按钮和系统菜单栏默认字体，统一图标与文字的垂直对齐。
- 应用使用 Codex 图标，双击打开时直接显示用量面板。
- 增加“在访达中显示应用”和“退出应用”，方便定位、关闭与删除。
- 完善可独立使用的构建、检查和打包脚本。

## 1.0.0

- 原生菜单栏用量监测、自动刷新、重置倒计时与旧数据提示。
